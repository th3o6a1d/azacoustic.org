# example netlify.toml
[build]
  command = "hugo"
  functions = "netlify/functions"
  publish = "public"

[build.environment]
  HUGO_VERSION = "0.146.5"

# Cypress plugin disabled - tests are outdated and need to be updated
# [[plugins]]
#   package = "netlify-plugin-cypress"
#
#   [plugins.inputs.postBuild]
#     enable = true
#
#   [plugins.inputs]
#     enable = false